function setup() {
    let myCanvas = createCanvas(400, 400);
    myCanvas.parent("sketch");
    background(102);
}

function draw() {
    stroke(255);
    textSize(32);
    text('coming soon', 10, 30);
}